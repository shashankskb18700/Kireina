import React from "react";
import { dbService } from "../../firebase/fbase";
const WW = ({ id }) => {
  console.log(id);
  // addDoc(collection(dbService.getFirestore(), 'shas01758@gmail.com'), {
  //   value: props.wishlistItem,
  //   createdAt: Date.now(),
  //   id: Date.now(),
  // });
};

export default WW;
